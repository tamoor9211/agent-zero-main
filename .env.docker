# Docker configuration for Agent Zero
# These ports will be used when starting Docker containers

# HTTP port for Agent Zero web interface
AGENT_ZERO_HTTP_PORT=55080

# SSH port for Agent Zero container access
AGENT_ZERO_SSH_PORT=55022

# Docker image to use (can be changed to local builds)
AGENT_ZERO_IMAGE=frdel/agent-zero-run:development
